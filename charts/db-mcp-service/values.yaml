image:
  repository: ghcr.io/gettuned/db-mcp-service
  tag: main
  pullPolicy: Always
  secret: github-registry-secret

resources:
  limits:
    cpu: 100m
    memory: 256Mi
  requests:
    cpu: 50m
    memory: 128Mi

app:
  name: db-mcp-service
  profiles: nonprod
  databaseNames: "user,offering,notification,intake"

