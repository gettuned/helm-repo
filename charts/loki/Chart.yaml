apiVersion: v1
appVersion: "3.2.0"
description: A Helm chart for Grafana Loki (single-binary mode, filesystem storage on EBS) — one shared instance in utils, serving all namespaces via multi-tenancy
name: loki
version: 0.1.0
