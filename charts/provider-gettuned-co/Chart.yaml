apiVersion: v1
appVersion: "1.0"
description: Dedicated chart for provider-gettuned-co — needs prod-only custom-domain catch-all routing that the generic web-app chart shouldn't carry for every consumer.
name: provider-gettuned-co
version: 1.0.0
